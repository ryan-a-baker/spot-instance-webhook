apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spot-instance-handler.fullname" . }}-test-connection"
  labels:
{{ include "spot-instance-handler.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "spot-instance-handler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
